# Explain Action

1. Read `context/current-feature.md` to understand what was implemented.
2. Run a workspace check (like `git diff main --name-only`) to get a list of files changed.
3. For each file created or modified, format the output exactly as follows:

## Files Changed

**path/to/file.ts** (new)
Brief explanation of what this file does and why it was added.

**path/to/other.ts** (modified)
What changed and why.

## How It All Connects

Brief summary of the data/control flow between these files.
